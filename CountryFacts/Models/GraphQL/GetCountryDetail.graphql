query GetCountryDetail($code: ID!) {
  country(code: $code) {
    code
    name
    native
    emoji
    continent {
      code
      name
    }
    capital
    currency
    languages {
      code
      name
    }
  }
}

